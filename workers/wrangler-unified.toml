# Configuración unificada para Workers de RW Consulting
# Este archivo es de referencia para configuración manual en Cloudflare

# ============================================
# rw-intake worker
# ============================================
# name = "rw-intake"
# main = "intake-worker.js"
# compatibility_date = "2024-11-20"
#
# kv_namespaces = [
#   { binding = "ESTUDIOS_KV", id = "ESTUDIOS_KV_ID", preview_id = "ESTUDIOS_KV_PREVIEW_ID" }
# ]
#
# [env.production]
# vars = { ENVIRONMENT = "production" }
#
# [env.preview]
# vars = { ENVIRONMENT = "preview" }

# ============================================
# rw-publish worker
# ============================================
# name = "rw-publish"
# main = "publish-worker.js"
# compatibility_date = "2024-11-20"
#
# [env.production]
# vars = { ENVIRONMENT = "production" }
#
# [env.preview]
# vars = { ENVIRONMENT = "preview" }

# ============================================
# rw-consulting-chat worker (existente)
# ============================================
# name = "rw-consulting-chat"
# main = "worker.js"
# compatibility_date = "2024-11-20"
#
# [env.production]
# vars = { ENVIRONMENT = "production" }
#
# [env.preview]
# vars = { ENVIRONMENT = "preview" }

# ============================================
# Instrucciones
# ============================================
# 1. Crear cada Worker por separado en Cloudflare
# 2. Usar la configuración correspondiente de arriba
# 3. Configurar Environment Variables según .env.example
# 4. Para rw-intake, crear KV namespace y agregar binding
#
# Ver CLOUDFLARE_SETUP.md para guía completa