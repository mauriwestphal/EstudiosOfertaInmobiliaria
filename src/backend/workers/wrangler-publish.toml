name = "rw-publish"
main = "workers/publish-worker.js"
compatibility_date = "2024-11-20"

[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview" }

# Mismo namespace que chat-worker — mismo id
[[kv_namespaces]]
binding = "CHAT_CREDITS"
id = "REPLACE_WITH_KV_NAMESPACE_ID"